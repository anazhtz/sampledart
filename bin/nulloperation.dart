// ?? double quation mark using there is no values in variable that time we can use ?? and entering"there is no value"
// null value case dart not accepting that time we can use like " int ? age;

void main(){
  int ? age;
  var res= age ?? "there is no value";
  print(res);

int ? number;
var res1= number ?? "25";
print(res1);

int ? age1;
var res3= age1 ?? "there is no value";
print(res3);


}